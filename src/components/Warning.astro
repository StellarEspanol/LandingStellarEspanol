---
interface Props {
  title?: string;
  type?: 'warning' | 'info' | 'error' | 'tip';
}

const { 
  title = 'Advertencia',
  type = 'warning'
} = Astro.props;

const types = {
  warning: {
    bgColor: 'bg-amber-50',
    borderColor: 'border-amber-300',
    titleColor: 'text-amber-800',
    textColor: 'text-amber-700',
    icon: '⚠️'
  },
  info: {
    bgColor: 'bg-blue-50',
    borderColor: 'border-blue-300',
    titleColor: 'text-blue-800',
    textColor: 'text-blue-700',
    icon: 'ℹ️'
  },
  error: {
    bgColor: 'bg-red-50',
    borderColor: 'border-red-300',
    titleColor: 'text-red-800',
    textColor: 'text-red-700',
    icon: '❌'
  },
  tip: {
    bgColor: 'bg-green-50',
    borderColor: 'border-green-300',
    titleColor: 'text-green-800',
    textColor: 'text-green-700',
    icon: '💡'
  }
};

const style = types[type];
---

<div class={`p-4 ${style.bgColor} border-l-4 ${style.borderColor} rounded-r-lg my-4`}>
  <div class="flex items-center">
    <span class="text-xl mr-2">{style.icon}</span>
    <p class={`font-medium ${style.titleColor}`}>{title}</p>
  </div>
  <div class={`mt-2 ${style.textColor}`}>
    <slot />
  </div>
</div>
